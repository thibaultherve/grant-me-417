import type { VisaType } from '../types';

// Single source of truth for visa labels
export const VISA_LABELS: Record<VisaType, string> = {
  first_whv: '1st WHV (417)',
  second_whv: '2nd WHV (417)',
  third_whv: '3rd WHV (417)',
};
