{
  "name": "get-granted-417",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:client": "pnpm --filter @get-granted/client dev",
    "dev:server": "pnpm --filter @get-granted/server start:dev",
    "build:client": "pnpm --filter @get-granted/client build",
    "build:server": "pnpm --filter @get-granted/server build",
    "build:shared": "pnpm --filter @get-granted/shared build",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "esbuild",
      "unrs-resolver",
      "bcrypt"
    ]
  }
}
